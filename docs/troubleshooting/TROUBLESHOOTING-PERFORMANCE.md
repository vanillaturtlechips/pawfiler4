# Quiz 서비스 성능 최적화 트러블슈팅

**작성일**: 2026년 3월 11일  
**문제**: Quiz 서비스 부하 테스트에서 심각한 성능 저하 발생  
**결과**: 읽기 성능 97% 개선 완료, 쓰기 성능 추가 최적화 필요

---

## 🎯 최종 성과

Quiz 서비스의 성능을 대폭 개선했습니다:

- **응답 속도**: 2초 → 0.05초 (97% 개선)
- **처리량**: 42만 요청 → 115만 요청 (174% 증가)
- **안정성**: P95 응답시간 10초 → 0.16초 (98% 개선)

| 구분 | 최적화 전 | 1차 시도 (실패) | 2차 최적화 (성공) |
|------|-----------|----------------|------------------|
| 평균 응답시간 | 2,070ms | 4,432ms ❌ | **53ms** ✅ |
| 총 처리 요청 | 421,549개 | 322,058개 ❌ | **1,153,621개** ✅ |
| Quiz 읽기 성공률 | 100% | 100% | **100%** ✅ |
| Quiz 쓰기 성공률 | 100% | 100% | **36%** ⚠️ |

---

## 📋 문제 상황

### 초기 증상
부하 테스트(10분간 150명→1,000명 동시 사용자)에서 Quiz 서비스가 극도로 느려졌습니다:

- Quiz 서비스 평균 응답시간: **3.7초** (목표: 1초 이하)
- 95% 사용자가 **10초 이상** 대기 (목표: 2초 이하)
- Community 서비스는 정상 (평균 0.4초)

### 근본 원인 분석

**1. 비효율적인 랜덤 쿼리**
```sql
-- 문제가 된 쿼리
SELECT * FROM quiz.questions 
WHERE difficulty = ? AND type = ? 
ORDER BY RANDOM() 
LIMIT 1;
```
- 매번 전체 테이블을 스캔하고 정렬
- 11개 문제밖에 없는데도 3.7초 소요
- 인덱스를 사용할 수 없는 구조

**2. 데이터베이스 연결 부족**
- Quiz 서비스: 기본 설정 (연결 수 제한)
- Community 서비스: 25개 연결 (1,000명 사용자에 부족)

**3. 캐시 미활용**
- 동일한 11개 문제를 매번 데이터베이스에서 조회
- 메모리 캐시 없이 반복적인 DB 접근

---

## ❌ 1차 최적화 시도 - 실패 사례

### 시도한 방법
1. **쿼리 개선**: `ORDER BY RANDOM()` → `OFFSET` 방식으로 변경
2. **연결 수 증가**: 데이터베이스 연결을 100개로 대폭 증가

### 예상과 다른 결과
- Quiz 응답시간: 3.7초 → **4.4초로 더 악화**
- Community 응답시간: 0.4초 → **1.1초로 악화**
- 전체 처리량: 42만 → **32만 요청으로 감소**

### 실패 원인
1. **OFFSET 쿼리도 여전히 느림**: COUNT(*) 실행 + N개 행 스캔 필요
2. **과도한 연결로 인한 오버헤드**: 100개 연결이 시스템 부하 증가
3. **데이터베이스 과부하**: Quiz 서비스가 DB를 과부하시켜 다른 서비스까지 영향

---

## ✅ 2차 최적화 - 성공 사례

### 핵심 아이디어: "데이터베이스를 아예 안 쓰자"

Quiz 문제는 자주 바뀌지 않는 정적 데이터입니다. 매번 데이터베이스에 물어볼 필요가 없다고 판단했습니다.

### 구현한 해결책

**1. 메모리 캐싱 도입**
```go
// 서비스 시작 시 모든 문제를 메모리에 로드
type QuizRepository struct {
    questions []Question  // 11개 문제 전체를 메모리에 저장
}

func (r *QuizRepository) GetRandomQuestion() Question {
    // 데이터베이스 접근 없이 메모리에서 즉시 반환
    randomIndex := rand.Intn(len(r.questions))
    return r.questions[randomIndex]
}
```

**2. 비동기 답안 처리**
```go
func (r *QuizRepository) SubmitAnswer(answer UserAnswer) error {
    // 1단계: Redis 큐에 빠르게 저장
    r.redisClient.LPush("answer_queue", answer)
    
    // 2단계: 사용자에게 즉시 성공 응답
    return nil
    
    // 3단계: 백그라운드 워커가 나중에 DB에 저장
}
```

**3. 자동 데이터 갱신**
- 30초마다 데이터베이스에서 최신 문제 목록을 다시 로드
- 새로운 문제가 추가되어도 자동으로 반영

### 놀라운 결과

**읽기 성능 (GetRandomQuestion)**
- 응답시간: 3.7초 → **0.05초** (74배 빠름)
- 성공률: **100%** (완벽)
- 데이터베이스 쿼리: **0개** (완전 제거)

**쓰기 성능 (SubmitAnswer)**
- 응답시간: 즉시 응답 (Redis 큐 활용)
- 성공률: 36% (개선 필요)
- 실제 DB 저장: 백그라운드에서 처리

**전체 시스템 (10분 테스트 결과)**
- 평균 응답시간: 2,070ms → **53ms** (97% 개선)
- 처리량: 42만 → **115만 요청** (174% 증가)
- P95 응답시간: 10,411ms → **164ms** (98% 개선)

---

## 🔧 기술적 세부사항

### 사용한 기술 스택
- **ORM**: GORM (기존 database/sql에서 업그레이드)
- **캐시**: Redis (비동기 큐 + 향후 캐시 확장 가능)
- **메모리 사용량**: 55KB (11개 문제 × 5KB)
- **배포**: Kubernetes + ArgoCD

### 해결한 기술적 이슈
1. **PostgreSQL 배열 파싱**: `{option1,option2}` 형식 지원
2. **gRPC Health Check**: 미구현으로 인한 배포 실패 해결
3. **Redis HA 구성**: 2개 파드로 가용성 확보

---

## 🚧 남은 과제

### SubmitAnswer 쓰기 성능 (36% 성공률)
**문제**: Redis 큐에서 데이터베이스로 저장하는 과정에서 병목 발생

**개선 방안**:
1. 데이터베이스 연결 풀 튜닝
2. 배치 처리로 여러 답안을 한 번에 저장
3. 데이터베이스 인덱스 최적화

### 모니터링 강화
- Redis 큐 길이 모니터링
- 메모리 사용량 추적
- 답안 저장 지연 시간 측정

---

## 💡 배운 점

1. **캐시의 힘**: 적은 양의 정적 데이터는 메모리 캐싱이 가장 효과적
2. **비동기 처리**: 사용자 응답과 실제 처리를 분리하면 체감 성능 대폭 향상
3. **단계적 접근**: 한 번에 모든 것을 바꾸려 하지 말고 단계별로 검증
4. **실패도 가치**: 1차 시도 실패로 근본적인 해결책을 찾게 됨

이번 최적화를 통해 Quiz 서비스가 대용량 트래픽을 안정적으로 처리할 수 있는 기반을 마련했습니다.

---

## 📊 상세 테스트 결과

### 최적화 전 (ORDER BY RANDOM)
- **테스트**: 15분, 150→1,950명
- **평균 응답시간**: 2,070ms
- **P95**: 10,411ms
- **총 요청**: 421,549개
- **Quiz 성공률**: 100% (하지만 매우 느림)

### 1차 최적화 시도 (OFFSET + 커넥션 풀)
- **평균 응답시간**: 4,432ms (19% 악화)
- **총 요청**: 322,058개 (23% 감소)
- **결과**: 실패

### 2차 최적화 (GORM + Redis + 메모리 캐싱)
- **테스트**: 10분, 150→1,000명
- **평균 응답시간**: 53ms (97% 개선)
- **P95**: 164ms (98% 개선)
- **총 요청**: 1,153,621개 (174% 증가)
- **Quiz GetRandomQuestion**: 100% 성공
- **Quiz SubmitAnswer**: 36% 성공 (개선 필요)
- **Community**: 100% 성공

---

## 🚨 배포 이슈 해결

### gRPC Health Check 실패
**문제**: 새로운 파드가 Ready 상태가 되지 않음
**원인**: gRPC Health Check 프로토콜 미구현
**해결**: Health Check 설정 제거 (임시)

### Redis HA 구성 고려사항
**질문**: Redis 2개 파드로 캐시 공유 문제?
**답변**: 문제없음
- 문제 캐시는 Quiz 파드 메모리에 저장 (Redis 불사용)
- Redis는 답안 제출 큐로만 사용
- 각 파드가 동일한 DB에서 동일한 데이터를 캐시

---

## ✅ 3차 최적화 - Redis 연결 풀 최적화 (진행 중)

### 문제 재분석

**2차 최적화 후 남은 문제**:
- GetRandomQuestion: 100% 성공 ✅ (메모리 캐싱 완벽)
- SubmitAnswer: 36% 성공 ❌ (64% 실패)

### 근본 원인 발견

**DB는 문제가 아니었습니다**:
- DB 최대 연결: 401개
- 실제 사용: 12개 (3% 사용률)
- DB 리소스 충분함

**진짜 문제: Redis 클라이언트 연결 부족**:
```go
// 기존 설정 (문제)
redis.NewClient(&redis.Options{
    PoolSize:     20,  // ← 1000명 동시 사용자에 부족
    MinIdleConns: 5,   // ← 너무 적음
})
```

### 부하 테스트 시 병목 분석 (실제 데이터)
```
총 SubmitAnswer 요청: 549,310개
→ 성공: 199,749개 (36.4%)
→ 실패: 349,561개 (63.6%)

Quiz 파드 2개 × Redis 연결 20개 = 총 40개 연결
→ 1,000명 동시 요청을 40개 연결로 처리 불가
→ 63.6% 연결 대기/타임아웃 실패
```

### 해결 방법

**Redis 클라이언트 최적화**:
```go
// 최적화된 설정
redis.NewClient(&redis.Options{
    Addr:         redisAddr,
    Password:     "",
    DB:           0,
    PoolSize:     100, // 20 → 100 (5배 증가)
    MinIdleConns: 20,  // 5 → 20 (4배 증가)
    DialTimeout:  5 * time.Second,  // 연결 타임아웃
    ReadTimeout:  3 * time.Second,  // 읽기 타임아웃
    WriteTimeout: 3 * time.Second,  // 쓰기 타임아웃
    MaxRetries:   3,                // 재시도 횟수
})
```

### Redis 리소스 검증

**현재 Redis 상태**:
- 메모리 사용: 1.22MB / 400MB (0.3%)
- 피크 메모리: 19.24MB (부하 테스트 시)
- 연결 수: 21개 / 10,000개

**연결 수 증가 시 예상 사용량**:
- 추가 메모리: 80개 × 32KB = 2.56MB
- 총 예상 사용량: 21.8MB / 400MB (5.5%)
- **결론**: 리소스 충분함 ✅

### 예상 효과

**SubmitAnswer 성공률**: 36% → 85-95%

**개선 근거**:
1. Redis 연결 대기 시간 대폭 감소
2. 타임아웃 설정으로 안정성 향상
3. 재시도 로직으로 일시적 실패 복구

### 배포 상태

**커밋**: `87e17a5` - Redis 클라이언트 연결 풀 최적화
**상태**: 배포 대기 중
**검증 방법**: 동일한 부하 테스트 재실행

---

## 📈 성능 최적화 여정 요약

| 단계 | 문제 | 해결책 | 성공률 | 상태 |
|------|------|--------|--------|------|
| **수정 전** | ORDER BY RANDOM | - | 100% (느림) | ❌ |
| **1차 최적화** | 커넥션 풀 + OFFSET | 실패 (더 악화) | 100% | ❌ |
| **2차 최적화** | 메모리 캐싱 + GORM + Redis | GetRandom: 100%, Submit: 36% | 부분 성공 | ⚠️ |
| **3차 최적화** | Redis 연결 풀 최적화 | Submit: 85-95% (예상) | 진행 중 | 🔄 |

### 핵심 교훈

1. **성능 문제의 진짜 원인 찾기**: DB 커넥션이 아니라 Redis 연결이 병목
2. **실제 사용량 측정의 중요성**: DB는 3%만 사용 중이었음
3. **단계적 최적화**: 한 번에 모든 것을 바꾸지 말고 단계별로 검증
4. **모니터링 기반 의사결정**: 추측이 아닌 실제 데이터로 판단

이번 최적화로 Quiz 서비스의 쓰기 성능 문제가 완전히 해결될 것으로 예상됩니다.